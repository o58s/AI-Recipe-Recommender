---

## 📦 Requirements

This project requires **Python 3.7 or higher**.

Before running the code, install the necessary Python packages. The main dependencies are:

- [`ultralytics`](https://pypi.org/project/ultralytics/) — YOLOv8 model inference  
- [`opencv-contrib-python`](https://pypi.org/project/opencv-python/) — Webcam and image processing

### Installation

You can install the required packages easily via pip:

```bash
pip install ultralytics opencv-python
